# Backend API origin.
# Development: leave empty to use Vite proxy → http://localhost:5000
# Production build (Netlify/Vercel/etc.): set to your deployed API, no trailing slash
# Example: VITE_API_URL=https://corporate-commitment-monitor-api.onrender.com
# VITE_API_URL=
