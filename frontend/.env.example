# Backend API origin. Leave empty in development to use Vite's proxy (vite.config.js).
# For production build, set to your API URL, e.g. https://your-api.example.com
# VITE_API_URL=http://localhost:5000
